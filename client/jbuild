(executable
 ((name       app)
  (libraries (async_js
              base
              core_kernel
              incr_dom

              protocol))
  (preprocess (pps (ppx_jane)))))

(install
 ((section share)
  (files ((app.bc.js as js/app.js)))))
